version: "3.7"
services:
  sflog:
    container_name: sflog
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - './node_modules:/app/node_modules'
    ports:
      - '3001:3000'
    environment:
      - NODE_ENV=development
